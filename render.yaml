services:
  - type: web
    name: workopencodex-sa
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn --workers 2 --threads 4 --timeout 60 --bind 0.0.0.0:$PORT sa:app
    healthCheckPath: /health
    autoDeploy: true
